## 第六次会议
### 本次会议主要展示这一周工作成果，以及决定下一周的任务

<font size=4>

#### 1.工作成果展示

release1.0版本发布，前后端全过程使用流程展示

#### 2.后续需要做的工作
##### 前端组
①用户体验优化
比如：
- 前端解析tags
- “历史记录”的删除按钮：不要直接显示。可以改成“左滑出现”，或者是“长按右上角出现一个×”（就像手机APP一样）
- 拖动公众号可以实现公众号排序（参考网络学堂）（这应当是纯前端的内容。可能需要用到缓存有关的知识（我不会）
- 阅读原文后，上方黑色栏显示能不能修复？
- 前端进行密码强度检测/安全性检测（不能用太常见的密码），并反馈给用户。后端的密码强度检测相关逻辑先保留，不用删除。可以参考后端的检测逻辑或修改
- ![alt text](image.png)
- 无论何时等待，都需要有加载动画。（而且由于涉及到爬虫任务，等待时间会比较长）。可以设计有意思的加载动画？比如可以设计一个我们的专属人物形象，加载的时候可以展示其动画（或者直接用软小宣也可以）
- key_info这个关键词似乎并没有用到？可以在summary上面加一行“关键词： 关键词1，关键词2”这样的逻辑（这个很简单）

##### 后端组
① 确定校内公众号有哪些（20-30个左右）
参考：
- https://yourschool.cc/thubook/gzh.html#%E5%90%84%E9%99%A2%E7%B3%BB%E5%AE%98%E6%96%B9%E5%85%AC%E4%BC%97%E5%8F%B7
- https://thu.services/accounts/


② 后端任务执行：我现在已经把爬虫弄成指令式的了，可以直接用python manage.py调用

    # 更新特定公众号
    python manage.py update 清华大学 北京大学 --count 5
    # 更新所有公众号
    python manage.py update_public_account
    # 给ai处理特定公众号的文章 （待实现）
    python manage.py process 清华大学 北京大学 --max_count 5
    # 给ai处理所有文章
    python manage.py process_articles

后续我们需要有更多的指令，便于我们后端做调试

还需要考虑到安全性、效率的问题，比如：
- 爬虫爬取公众号信息后会出现多余的相关公众号，那些无人订阅的公众号，是不是不需要持续更新了？等到有人订阅时再更新（可以设置is_active字段来决定，后续我会处理）
- 爬虫频率控制、错误处理......
- 处理文章时，如果遇到报错（比如之前我传到群里的），需不需要做立即重传？还是等下次再一起做？
- 限制前端过于频繁调用爬虫
- ......

等到这些都解决后，我们就可以设置一些定时执行的指令了。后续服务器就是定时执行各个指令即可。

③ 助教需求对接

@王雪松 跟助教许德成对接一下他的需求，后续我们再考虑一下要怎么实操，然后分工。

④ 设计测试用例

由于我添加了一些新的API，可以再做一些测试用例，主要是针对各个API做测试。有问题及时排查并反馈。

期望在第12周周末能够把release2.0做出来并部署!这应当是一个能稳定运行的版本

