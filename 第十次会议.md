## 第十次会议

#### 1.工作成果展示

##### 1.效率测试
这周我进行了效率测试（使用locust）。大致有以下问题：

①user的api读测试（并发50）：大部分调用速度还可以，在几十ms量级，但是访问历史记录的效率很差，需要5s左右，我现在做了分页优化，后续要重新测试。
②user的api写测试（并发20）：效率上还可以，大部分都在几十ms，有一些被历史记录获取给限制了。
②静态资源并发访问速度奇差，经过排查后发现问题不在于服务器端。
③文章推送效率测试（并发50）：效率比较差，平均延迟大约在2.5s级别，现已被优化至0.7~1.3s.

##### 2.前端bug测试
前端的代码质量太差太差，变量命名、注释、缩进全部不统一，而且我稍微用一下就有很多bug，我已经做了一些修复，具体可以查看github的提交。后续前端组需要有人做测试，检测正确性（后续期末提交测试文档要用到）。

#### 2.后续要做的事情：

目前明显还有问题的地方：
- 帮助页面要完成（纯文字描述即可）
- AI页面应当解析markdown、要显示已经思考的时长，那个跳动的点的位置也不对（我加入流式传输后还没来得及修复），需要纠正一下。
- 由于我们小程序是个体认证，发布app后用户没法跳转到原文链接，可能需要做一些取舍，比如如果无法跳转，应当告诉用户原文链接，让用户手动复制去微信跳转。
- 用户交互体验优化：有一些需要加载的地方还是没有加载动画，需要排查；比如修改资料时悬浮窗直接出现，可能需要加一个过渡动画。

接下来的工作：
①写测试文档（后端 + 前端都需要做测试，我已有的测试数据有的是没做优化的结果，后续要重新优化，需要指定一个人写测试文档wxs做）

③期末展示PPT：gr做，需要重点突出以下内容：
- 功能与原生态微信管理工具比，优势在哪？（我们的特色）
- 开发框架
- 基本页面介绍，（可以放几个gif展示一些功能）
- 效率测试结果
- 演示视频
- 重难点问题

④前端代码问题排查/代码测试/用户体验优化：ych做，日常任务

前端不需要写端到端测试，而是手动进行一个“全面测试”，需要有一个表记录测试结果。可以按“页面——组件——功能”的逻辑组织表格。以首页为例（草图，不一定按照这个逻辑来）：

(一) 首页：轮播图、文章列表、tabBar、桌宠......

1.轮播图的功能：
① 访问原文链接
② 左右滑动，切换图片

2.文章列表的功能：
① 上下滚动
② 加载新文章

3.tabBar的功能：
① 跳转到校内页面
② 跳转到自选页面
③ 跳转到个人资料页面

桌宠的功能：
① 拖动
② 点击进入桌宠页面

**下面需要特别注意：**
① 对于每个功能，你需要描述其预期行为，然后记录是否符合预期行为（√/×）
② 预期行为是这个功能有什么限制？应当在什么时候触发？不需要太复杂，比如
- 桌宠的拖动：正确设置拖动边界，拖动不应该引起别的组件位置变化......
- 文章列表加载新文章：原有文章保持正常？新文章是否正确加载？加载到底后是否隐藏“加载更多”按钮，并提示加载完毕......

你需要特别关注的是“边界行为”，比如轮播图播到最后，可以回到第一个吗？文章加载完后，”加载更多“按钮会被隐藏，并显示文章加载完毕吗？
③ 遇到bug可以不着急修改，先记录，再统一修复
④ 每个页面只记录当前页面的功能，比如虽然首页可以进入桌宠，但是桌宠内部的功能应当记录在“桌宠页面”而不是首页里















