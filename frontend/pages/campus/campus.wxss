page {
  background-color: rgba(239, 239, 239, 0.6);
  height: 100vh;
  display: flex;
  flex-direction: column;
  overflow: hidden; /* 避免页面整体滚动，由article-list容器内部滚动 */
}

/* 顶部导航栏：原有样式不变，仅补充box-sizing */
.custom-navbar {
  position: fixed;
  width: 100%;
  height: 170rpx;
  background: #ffffff;
  display: flex;
  align-items: flex-end;
  justify-content: center;
  padding-bottom: 20rpx;
  box-sizing: border-box;
  z-index: 1000;
  color: #591bb7;
  font-weight: 600;
}
.navbar-title {
  font-size: 35rpx;
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
}
.alllist-icon {
  width: 50rpx;
  height: 50rpx;
  position: absolute;
  left: 30rpx;
  bottom: 17rpx;
}

/* 搜索栏整体：关键修改——去掉92%宽度和auto margin，改为100%宽度+内边距 */
.search-bar {
  display: flex;
  align-items: center;
  width: 100%; /* 改为100%宽度 */
  margin-top: 200rpx; /* 去掉auto，左右无margin */
  padding: 0 30rpx; /* 用内边距代替margin，保持和导航栏一致的左右间距 */
  box-sizing: border-box; /* 内边距包含在宽度内 */
  gap: 16rpx;
  flex-shrink: 0; /* 固定高度，不被压缩 */
}

/* 输入框：原有样式不变 */
.search-box {
  flex: 1;
}
.search-inner {
  display: flex;
  align-items: center;
  background-color: #fff;
  border: 2rpx solid #7b2ff7;
  border-radius: 50rpx;
  height: 70rpx;
  padding: 0 16rpx 0 24rpx;
}
.search-icon {
  width: 36rpx;
  height: 36rpx;
  margin-right: 14rpx;
}
.search-input {
  flex: 1;
  font-size: 26rpx;
  color: #333;
  border: none;
  background: transparent;
  padding-right: 10rpx;
}
.search-placeholder {
  color: #aaa;
}

/* 搜索文字按钮样式：原有样式不变 */
.search-text-btn {
  font-size: 26rpx;
  color: #fff;
  background-color: #7b2ff7;
  padding: 8rpx 20rpx;
  border-radius: 30rpx;
  margin-left: 10rpx;
  white-space: nowrap;
  transition: all 0.2s;
}
.search-text-btn:active {
  background-color: #6a1cd6;
  transform: scale(0.98);
}

.filter-icon {
  width: 50rpx;
  height: 50rpx;
}

/* article-list 容器 */
.article-list-container {
  flex: 1; /* 占满搜索栏到底部导航栏的所有剩余空间 */
  width: 100%;
  box-sizing: border-box;
  padding: 0 0;
  overflow: auto; /* 内容过多时内部滚动，不影响页面 */
  -webkit-overflow-scrolling: touch; 
  padding-bottom: 0; 
}
  
/* 空状态提示 */
.empty-tip {
  text-align: center;
  color: #aaa;
  font-size: 26rpx;
  padding: 80rpx 0;
  /* 空状态居中 */
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100%;
}

/* 筛选弹窗相关：原有样式不变 */
.filter-mask {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.4);
  z-index: 999;
  backdrop-filter: blur(2rpx);
}
.filter-popup {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 95%;
  height: 75%;
  background-color: #fff;
  border-radius: 30rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.15);
  z-index: 1000;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}
.popup-header {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 35rpx 100rpx;
  border-bottom: 2rpx solid #e0e0e0;
  background-color: #fff;
  position: relative;
  flex-shrink: 0;
}
.popup-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  text-align: center;
  flex: 1;
}
.close-icon {
  position: absolute;
  right: 35rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 32rpx;
  height: 32rpx;
  z-index: 1;
}
.filter-container {
  flex: 1;
  display: flex;
  height: 100%;
  overflow: hidden;
}
.category-nav {
  width: 143rpx;
  background-color: #f8f8f8;
  border-right: 1rpx solid #e0e0e0;
  overflow-y: auto;
  flex-shrink: 0;
}
.category-item {
  padding: 28rpx 20rpx;
  font-size: 26rpx;
  color: #666;
  text-align: center;
  border-bottom: 1rpx solid #e0e0e0;
  transition: all 0.3s;
}
.category-active {
  background-color: #fff;
  color: #7b2ff7;
  font-weight: bold;
  border-left: 4rpx solid #7b2ff7;
}
.category-content {
  flex: 1;
  padding: 20rpx;
  display: flex;
  flex-direction: column;
  height: 100%;
  box-sizing: border-box;
  overflow: hidden;
}
.time-input-group {
  margin-bottom: 30rpx;
}
.time-label {
  font-size: 28rpx;
  color: #333;
  margin-bottom: 30rpx;
  font-weight: 600;
  padding-left: 4rpx;
}
.time-input {
  border: 2rpx solid #e0e0e0;
  border-radius: 24rpx;
  padding: 20rpx;
  font-size: 26rpx;
  background-color: #fafafa;
  width: 100%;
  box-sizing: border-box;
}
.tag-row {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
}
.tag-item {
  width: calc(50% - 10rpx);
  padding: 18rpx 30rpx;
  background-color: #fafafa;
  border-radius: 24rpx;
  font-size: 24rpx;
  color: #333;
  text-align: center;
  border: 2rpx solid #e0e0e0;
  transition: all 0.3s;
  box-sizing: border-box;
}
.tag-selected {
  background-color: #a277e6;
  color: #fff;
  border-color: #7b2ff7;
  box-shadow: 0 4rpx 12rpx rgba(123, 47, 247, 0.3);
}
.filter-actions {
  display: flex;
  border-top: 2rpx solid #e0e0e0;
  padding: 40rpx 60rpx;
  background-color: #fff;
  gap: 60rpx;
  flex-shrink: 0;
}
.filter-btn {
  flex: 1;
  padding: 22rpx;
  text-align: center;
  font-size: 28rpx;
  border-radius: 15rpx;
  font-weight: 600;
  transition: all 0.3s;
}
.reset-btn {
  background-color: #f5f5f5;
  color: #666;
  border: 2rpx solid #e0e0e0;
}
.reset-btn:active {
  background-color: #e8e8e8;
}
.confirm-btn {
  background-color: #7b2ff7;
  color: #fff;
  border: 2rpx solid #7b2ff7;
}
.confirm-btn:active {
  background-color: #6a1cd6;
  border-color: #6a1cd6;
}

/* 底部导航栏：原有样式不变，补充z-index */
.tab-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 120rpx;
  background-color: #fff;
  display: flex;
  justify-content: space-around;
  align-items: center;
  border-top: 1rpx solid #eee;
  box-shadow: 0 -2rpx 10rpx rgba(0, 0, 0, 0.05);
  z-index: 100;
  flex-shrink: 0; /* 固定在底部，不被压缩 */
}
.tab-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  color: #666;
  font-size: 26rpx;
}
.tab-item .menu-icon {
  width: 40rpx;
  height: 40rpx;
  margin-bottom: 6rpx;
  filter: brightness(0) saturate(100%) invert(62%) sepia(0%) saturate(0%) hue-rotate(201deg) brightness(88%) contrast(85%);
}
.tab-item.active {
  color: #7b2ff7;
  font-weight: bold;
}
.tab-item.active .menu-icon {
  filter: brightness(0) saturate(100%) invert(33%) sepia(74%) saturate(6221%) hue-rotate(253deg) brightness(96%) contrast(101%);
}

/* 加载动画相关：原有样式不变 */
.loading-animation-container {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background-color: rgba(255, 255, 255, 0.8);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 9999 !important;
}
.loading-gif {
  width: 120rpx;
  height: 120rpx;
}
.loading-tip {
  text-align: center;
  padding: 40rpx 0;
  color: #999;
  font-size: 28rpx;
  display: none;
}

/* 公众号标签相关：原有样式不变 */
.account-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  width: 100%;
  overflow: hidden;
}
.tag-scroll-view {
  width: 100%;
  flex: 1;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  padding-right: 10rpx;
  box-sizing: border-box;
}
.account-content .tag-row {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
  width: 100%;
}
.account-content .tag-item {
  width: calc(50% - 10rpx);
  flex-shrink: 0;
  padding: 18rpx 20rpx;
  font-size: 24rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  box-sizing: border-box;
}
.tag-scroll-view::-webkit-scrollbar {
  width: 4rpx;
}
.tag-scroll-view::-webkit-scrollbar-thumb {
  background-color: #eee;
  border-radius: 2rpx;
}